fragment ProductListItemFragment on Product {
    id
    name
    description
    slug
    categories (first: 1) {
        name
        slug
    }
    images (first: 1) {
        url
    }
    price
}